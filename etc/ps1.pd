use Sys::Hostname;
$VAR1 = 
{
    '\w'   =>  sub { $ENV{PWD} },
    '\h'   =>  sub { hostname },
    '\u'   =>  sub { (getpwuid($>))[0] },
    '\W'   =>  sub { (split/\//,$ENV{PWD})[-1] },
    '\U'   =>  sub { [getpwnam(getlogin)]->[2]?'$':'#' }, # this should be \$, but i can't seem to get escape it right
}
