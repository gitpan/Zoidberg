$VAR1 = {
	module => 'Zoidberg::Fish::Commands',
	config => { max_dir_hist => 10 },
	commands => {
			back      => 'cd(q/<-/)',
			forw      => 'cd(q/->/)',
	},
	export => [qw/
		cd		exec		eval
		command		false		fc
		getopts		newgrp		pwd
		read		true		umask
		wait		set 		setenv
		_delete_object	_load_object	_hide
		_unhide		quit		source
		alias		unalias		unset

		dirs		popd		pushd
	/],
}
